import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certify-yaself.giflist',
  appName: 'giflist',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
